
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle pull-right" data-toggle="collapse" data-target="#navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
       <%unless (logged_in1?) ||(logged_in2?)%>
      <%=link_to "Placement Cell, IIIT-Kalyani", root_path ,class: "navbae-brand" ,id: "logo"%>
      <%end%>
      </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse " id="navbar-ex1-collapse" role="navigation">
      <ul class="nav navbar-nav navbar-right">
         <%unless (logged_in1?) ||(logged_in2?)%>
        <li><%=link_to "HOME",root_path,id:"nav_font"%></li>
         <%end%>
          <%unless (logged_in1?) ||(logged_in2?)%>
         
            <li><%=link_to "Why IIIT-Kalyani",why_path%></li>
          
         <%end%>
         <%if ((!logged_in1?)&&(!logged_in2?))%>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Company Section <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <%if logged_in2?%>
            <%else%>
            <li><%=link_to "Signup",new_company_path,class: "glyphicon glyphicon-user"%></li>
            <li role="separator" class="divider"></li>
            <li><%=link_to "Login",login2_path,class: "glyphicon glyphicon-log-in"%></li>
            <% end%>
          </ul>
        </li>
         <%end%>
         <%if (!logged_in2?)%>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Student Section <span class="caret"></span></a>
          <ul class="dropdown-menu">
        <% if logged_in1? %>
         
        <%else %>
           <li><%=link_to "Signup",new_student_path,class: "glyphicon glyphicon-user"%></li>
           <li role="separator" class="divider"></li>
            <li><%=link_to "Login",login1_path,class: "glyphicon glyphicon-log-in"%></li>
         <%end%>
          </ul>
        </li>
        <%end%>
       <%unless (logged_in1?) ||(logged_in2?)%>
        <li><%=link_to "Contact",about_path%></li>
           <%end%>
           <% if logged_in1? %>
         <li style="margin:4px 3px 2px 2px"><%= link_to 'Logout', logout1_path, method: :delete %></li>
         <%end%>
         <% if logged_in2? %>
         <li style="margin:4px 3px 2px 2px"><%= link_to 'Logout', logout2_path, method: :delete  %></li>
         <%end%>
      </ul>
       <%if (logged_in1?)%>
          <ul class="nav navbar-nav navbar-left">
         <li><%=link_to "Student",student_path(current_student),id:"nav_font"%></li>
          </ul>
           <%end%>
         <%if (logged_in2?)%>
          <ul class="nav navbar-nav navbar-left">
           <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">Company
              <span class="caret"></span></a>
               <ul class="dropdown-menu">
                 <li><%=link_to "Profile",company_path(current_company),id:"nav_font"%></li>
                 <li><%= link_to "Edit ", edit_company_path(current_company) %><li>
               </ul>
          </li>
          <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">Jobs
              <span class="caret"></span></a>
               <ul class="dropdown-menu">
                 <li><%= link_to "New ", new_job_path %></li>
               </ul>
          </li>
           <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">Exam
              <span class="caret"></span></a>
               <ul class="dropdown-menu">
               <li> <%= link_to 'Rules' , new_exam_path%></li>
               <li><%= link_to 'Show', @exam %><li>
               </ul>
          </li>
           </ul>
           <%end%>
  
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>